services:
  app:
    build:
      context: .
      dockerfile: src/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - todo-data:/data
      - ./src:/app/src
    command: npm run dev

volumes:
  todo-data: